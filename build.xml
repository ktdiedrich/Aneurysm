<project default="compile">
	<property file="build.properties"/>
    <target name="compile">
        <mkdir dir="${build.dir}"/>
        <javac srcdir="." destdir="${build.dir}" debug="true"/>
    </target>
</project>
